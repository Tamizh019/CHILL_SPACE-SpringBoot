# Server Configuration
server.port=${PORT:9195}

# Database Configuration (MySQL)
# Database Configuration (Previous Env Config)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Database Configuration (Aiven Cloud)
# spring.datasource.url=jdbc:mysql://chillspace-db-jefftamizh01-7fb8.h.aivencloud.com:18358/chill_space_db?sslMode=REQUIRED
# spring.datasource.username=avnadmin
# spring.datasource.password=AVNS_QCQeQhIS-uW8Ez91Y9q
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=ERROR

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Supabase Configuration
supabase.url=${SUPABASE_URL}
supabase.key=${SUPABASE_KEY}

# AI Configuration (Gemini)
gemini.api.key=${GEMINI_API_KEY}
gemini.model=gemini-2.0-flash-exp
